{
  "name": "transcribe-api",
  "version": "1.0.0",
  "description": "Node.js API for receiving and displaying data via POST and GET requests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["api", "express", "node"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
